package org.springboot.Selenium.flights;

import org.springframework.test.context.TestPropertySource;

@TestPropertySource(properties = "app.locale=flight_sp")
public class FlightEnTest extends FlightTest{
}
